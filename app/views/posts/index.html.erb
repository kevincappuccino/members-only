<h1>Listing all posts: </h1>

<p>
<%= link_to 'Create new post', new_post_path %>
</p>
<table>
<tr>
  <th>Title</th>
  <th>Body</th>
</tr>
  <% @posts.each do |post|%>
    <tr>
      <td><%= post.title %></td>
      <td><%= post.body %></td>
    </tr>
  <%end%>
</table>